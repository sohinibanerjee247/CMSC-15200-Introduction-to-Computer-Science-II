#include <stdlib.h>
#include <stdio.h>
#include "waves.h"

int main(int argc, char *argv[]) {
    draw_waves(200, 0, 0);
    // draw_waves(200, 50, 50);
    // draw_waves(200, 0, 50);
    // draw_waves(200, 50, 0);
    // draw_waves(500, -50, -100);
    return 0;
}